# AF3A Executable Hypothesis Materialization

AF3A materializes the repaired frozen AF2A plan into 44 genuine AF1
`ResearchHypothesis` definitions. It uses only canonical completed Candle windows
and performs no dataset access, forward-return calculation, or empirical screen.

Frozen inputs:

- Catalog ID: `060147848ab0d3aa3deb4a77fe0cf5af6bdd10a374587c24e5ff7a640a0a5d74`
- FDR-plan ID: `32a6a1e1e916a0b012bd5ac22d747b3b57afa53a99e5153d0fb1a0685511a6ae`
- Retained concepts: 15
- Materialized definitions: 44
- Planned registrations: 616

`materialization.json` is derived from the verified catalog, verified FDR plan,
and official executable materialization. Its loader reconstructs those definitions
and rejects self-resealed semantic drift.

## AF3B entry gate

AF3B may proceed only when all 44 definitions map one-to-one to the frozen plan,
the manifest verifies, callback sentinel tests pass, no extra definitions exist,
the two frozen IDs remain unchanged, and the complete repository suite passes.

AF3B may then resolve and verify the exact BTCUSDT and ETHUSDT DEVELOPMENT
datasets, construct two AF1 `ResearchDataset` values, declare the dataset-bound
AF1 FDR universe, and independently prove exactly 616 `FdrTestKey` values. AF3B
must stop before empirical screening.
